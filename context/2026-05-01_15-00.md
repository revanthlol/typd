# typd — Context Snapshot
**Date:** 2026-05-01 15:00
**Project:** typd — Wayland virtual keyboard + suggestion system (C / Wayland)
**Repo:** https://github.com/revanthlol/typd

---

## Current Phase
**Phase 03 — Suggestion strip on keyboard UI, Trie engine working**
Status: Complete

---

## Files State

### Implemented (non-empty, has real logic)
- `src/main.c` — Entry point, Wayland registry handling, event loop, and global binding.
- `src/virtual_kbd.c` — Key injection via `zwp_virtual_keyboard_v1`, layer shell window management, and input handling.
- `src/renderer.c` — Cairo-based rendering for the virtual keyboard and suggestion strip.
- `src/suggestions.c` — Shared engine implementing Trie for prefix autocomplete and BK-Tree for fuzzy correction.
- `tests/test_trie.c` — Unit tests for prefix autocomplete.
- `tests/test_bktree.c` — Unit tests for fuzzy correction.

### Stubs (empty or skeleton only)
- `src/config.c` — pending (Phase 08)
- `src/context_detect.c` — pending (Phase 07)
- `src/input_method.c` — pending (Phase 05)
- `src/popup.c` — pending (Phase 05)
- `tests/test_context.c` — pending

### Headers
- `include/common.h` — Shared AppState and core types.
- `include/virtual_kbd.h` — Virtual keyboard structures and function declarations.
- `include/suggestions.h` — Suggestion engine types and API.
- `include/renderer.h` — Drawing function prototypes.
- `include/layout.h` — QWERTY key layout definitions.

### Build (meson.build)
- Sources wired: `main.c`, `virtual_kbd.c`, `renderer.c`, `suggestions.c` + generated protocols.
- Dependencies declared: `wayland-client`, `cairo`, `xkbcommon`.
- Protocol XMLs scanned: `wlr-layer-shell-unstable-v1.xml`, `xdg-shell.xml`, `virtual-keyboard-unstable-v1.xml`.

---

## Protocols

| Protocol | XML Present | Header Generated | Bound in Code |
|----------|-------------|-----------------|---------------|
| wlr-layer-shell-unstable-v1 | ✅ | ✅ | ✅ |
| zwp_virtual_keyboard_v1 | ✅ | ✅ | ✅ |
| zwp_input_method_v2 | ❌ | ❌ | ❌ |
| zwp_input_popup_surface_v2 | ❌ | ❌ | ❌ |
| zwp_text_input_v3 | ❌ | ❌ | ❌ |

---

## Suggestion Engine

| Component | Status | Notes |
|-----------|--------|-------|
| Trie (prefix autocomplete) | ✅ | Fully implemented and tested. |
| BK-Tree (fuzzy correction) | ✅ | Fully implemented and tested. |
| words.freq loaded | ✅ | Loaded in `main.c`. |

---

## Recent Git Commits
- 268a523 fix(phase-1): overlay floats correctly, clean signal exit
- 06e9c29 chore: add context-snapshot skill for LLM agent handoff
- 24f2c44 chore: initial project scaffold

---

## Known Issues / TODOs
- `zwp_text_input_v3` and `zwp_input_method_v2` protocols are missing from `protocols/` but required for upcoming phases.
- `AppState` missing members for these protocols.
- `config.c` is a stub; settings are currently hardcoded in `virtual_kbd.init`.

---

## Next Task
**What to implement next (be specific):**

> Fetch `text-input-unstable-v3.xml` and `input-method-unstable-v2.xml` from `wayland-protocols` system directory and add them to the `protocols/` folder. Update `meson.build` to generate the client-protocol headers and code for these. Then, update `AppState` in `include/common.h` and the registry listener in `src/main.c` to bind these globals. This is a prerequisite for Phase 04 and Phase 05.

---

## Agent Feed Summary
> typd is a C11 Wayland-native virtual keyboard + suggestion daemon. Current phase: 03 — Suggestion strip complete. Implemented: main.c, virtual_kbd.c, renderer.c, suggestions.c (Trie + BK-Tree). Pending: input_method.c, popup.c, config.c, context_detect.c. meson.build wires core sources and 3 protocols. Protocols bound: layer-shell, virtual-keyboard, xdg-shell. Next task: Fetch and bind text-input-v3 and input-method-v2 protocols. Constraints: single binary, no GTK/Qt, Cairo rendering, wlroots compositors only, target <2MB stripped <15MB RAM.
